PORT=5000
NODE_ENV=development
API_PREFIX=/api/v1
FRONTEND_URL=http://localhost:3000

# Database Connection URL
# SQLite (Local zero-config dev file database):
DATABASE_URL="file:./dev.db"

# PostgreSQL (Production / Cloud databases like Supabase, Neon, AWS RDS, Docker):
# DATABASE_URL="postgresql://postgres:password@localhost:5432/productivity_db?schema=public"
